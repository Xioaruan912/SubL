# SubLinkX · GPT 工作清单

> 本文件是当前唯一给 GPT 使用的项目工作清单。更新时间：2026-08-29。

## AI 读取范围

- GPT 只读取本文件作为项目待办与进度来源。
- `new_todo.md` 是历史记录/旧 AI 交接内容，已明确排除，不得再次执行、合并或当作未完成事项。
- `todo/`、临时日志、构建产物和 VPS 备份同样不作为待办来源。

## 已完成并已验证

### 前端与体验

- 完成现代化控制台、节点地图、存活率仪表盘和质量详情。
- 节点分组选择改为可展开分组后选择具体节点；节点构建不允许空分组。
- 节点卡片的解锁/TCP 状态已限制在卡片布局内。
- 模板列表升级为 Monaco 编辑器、结构导航、关键词/行定位、服务端 YAML 校验。
- 模板编辑支持版本历史、版本对比、原子保存、回滚；删除/修改前自动保留快照。
- 新增“测试 → 本机分流检测”页面，显示目标 favicon（失败时使用本地图标兜底）、出口汇总、地区、响应时间和失败原因。
- 新增“订阅节点分流检测”：选择订阅，再选择具体节点，服务端通过该节点访问目标站点。
- 新增“按模板规则验证”：读取订阅绑定的 Clash/Surge/Loon 模板，按 DOMAIN/DOMAIN-SUFFIX/DOMAIN-KEYWORD/MATCH 规则匹配站点；Gemini 默认 JP、ChatGPT/OpenAI 默认 US，存在多个候选时按质量分数和延迟选最优节点，并返回命中规则、实际节点、候选数、地区、出口结果。

### 订阅处理

- 订阅支持主源/备用源，逐个尝试直到第一个可用的 2xx 非空响应。
- 新增处理链：包含/排除正则、重命名、协议过滤、去重、名称/国家/延迟/质量排序、节点数量上限。
- 新增处理链预览及前后数量、拒绝原因统计。
- 所有输出客户端在合并分组节点后统一应用处理链。
- 机场源失败时更新订阅可回退到最近一次有效节点集合。

### 节点质量、解锁与告警

- 质量统计新增 P95 延迟、连续失败次数、置信度；历史窗口支持 24 小时、7 天、30 天。
- 新增节点 down/recovery 事件、Webhook 告警、失败阈值和维护时段。
- 新增稳定、低延迟、AI、流媒体四类场景推荐，结合质量和解锁样本给出理由。
- 解锁检测改为可注册检测器，并区分 available/blocked/unknown；检测结果保存为观察样本供推荐使用。
- 保留现有 sing-box 解锁/TCP 逻辑，不破坏原有客户端输出。

### 交付与线上状态

- Go 全量测试通过。
- Vue 生产构建与 `vue-tsc --noEmit` 通过。
- GitHub `main` 已推送提交：`14027eb`（上一版本）；图标/Gemini 版本为 `3a742d9`，模板规则验证版本待本次发布。
- VPS `/usr/local/bin/ppeelink` 已完成备份后原子替换，`ppeelink.service` 为 active。
- 线上首页、分流 chunk、Monaco worker、菜单、推荐、告警、模板校验接口均返回 200。
- VPS 真实订阅节点分流检测已验证 15 个目标，15/15 返回成功；Gemini 使用可达性验证，不虚报出口 IP。
- 当前生产备份：`/root/backup/ppeelink_suite_20260829_210931`。
- VPS 上 `telegram-video-forwarder` Docker Bot 已确认 running，重启策略为 `unless-stopped`，日志显示 Bot started。

## 当前待完成

1. 模板规则验证、favicon 加载、解锁/TCP 可达节点筛选和 Gemini 严格可用性判断已完成并部署（提交 `48ca4ea`）。
2. 检查不同模板格式及外部 RULE-SET：当前安全地标记为未解析，不会在服务端请求未知远程规则。
3. 后续可选增强：将分流目标列表改为数据库配置，允许管理员增删目标和设置检测路径；当前不影响既有功能。

## 线上操作纪律

- 任何 VPS 替换前必须先备份二进制、`db/` 和 `template/`。
- 新二进制必须先完成本地 `go test ./...`、前端构建和 SHA256 校验。
- 替换后必须检查 systemd active、首页 200、关键静态资源 200、关键 API 和日志；失败自动恢复备份二进制。
- 不修改或删除生产数据库中的现有节点、订阅、模板数据。
